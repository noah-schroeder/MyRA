{
  "name": "@karen/bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "karen-bridge": "./dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node --experimental-strip-types src/index.ts",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@karen/protocol": "*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/ws": "^8.5.13",
    "typescript": "^5.7.2"
  }
}
